# Nav2 参数 (Full Plan D3 + 实车调优)
# 关键点: 工厂 FW 不支持 Mecanum 横移 (vy 忽略), 所以按差速车配置.
#        机器人半径 0.18m (底盘 280mm + 留 20mm)
# 2026-04-25 改: OmniMotionModel → DifferentialMotionModel
#   原因: 工厂 FW 是 diff-drive, vy 命令无效, 横移不可达.
#         AMCL 粒子滤波按差速假设, 精度更高, 收敛更快.

amcl:
  ros__parameters:
    robot_model_type: nav2_amcl::DifferentialMotionModel  # 差速 (工厂 FW 限制)
    max_particles: 2000
    min_particles: 500
    laser_max_range: 8.0
    laser_min_range: 0.10
    update_min_d: 0.05
    update_min_a: 0.05
    scan_topic: /scan_lidar  # 避开 ESP32 factory FW 空数据的 /scan (见 5802ee0)

controller_server:
  ros__parameters:
    controller_frequency: 20.0
    FollowPath:
      plugin: "nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController"
      desired_linear_vel: 0.30  # 0.3 m/s 初始, 稳定后可提到 0.4-0.5
      lookahead_dist: 0.4
      min_lookahead_dist: 0.2
      max_lookahead_dist: 0.6
      transform_tolerance: 0.1
      use_velocity_scaled_lookahead_dist: true

local_costmap:
  local_costmap:
    ros__parameters:
      robot_radius: 0.18
      resolution: 0.05
      update_frequency: 5.0
      publish_frequency: 2.0
      plugins: ["obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: true
        observation_sources: scan
        scan:
          topic: /scan_lidar  # 同 amcl, 避开 ESP32 空 /scan
          sensor_frame: laser_link
          data_type: "LaserScan"
          marking: true
          clearing: true
          raytrace_max_range: 3.0
          obstacle_max_range: 2.5
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        inflation_radius: 0.20  # ≥ 0.186 inscribed (机器人半径), 否则 controller 报警

global_costmap:
  global_costmap:
    ros__parameters:
      robot_radius: 0.18
      resolution: 0.05
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: true
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: true
        observation_sources: scan
        scan:
          topic: /scan_lidar
          sensor_frame: laser_link
          data_type: "LaserScan"
          marking: true
          clearing: true
          raytrace_max_range: 3.0
          obstacle_max_range: 2.5
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        inflation_radius: 0.20

planner_server:
  ros__parameters:
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_navfn_planner::NavfnPlanner"
      use_astar: true  # A* 比 Dijkstra 更适合稀疏场地
      allow_unknown: false

# 复制 nav2_bringup 默认 collision_monitor 配置, 仅改 scan topic = /scan_lidar.
# (空 polygons 会 SIGABRT — 必须给至少一个 polygon)
collision_monitor:
  ros__parameters:
    base_frame_id: "base_footprint"
    odom_frame_id: "odom"
    cmd_vel_in_topic: "cmd_vel_smoothed"
    cmd_vel_out_topic: "cmd_vel"
    state_topic: "collision_monitor_state"
    transform_tolerance: 0.2
    source_timeout: 1.0
    base_shift_correction: True
    stop_pub_timeout: 2.0
    polygons: ["FootprintApproach"]
    FootprintApproach:
      type: "polygon"
      action_type: "approach"
      footprint_topic: "/local_costmap/published_footprint"
      time_before_collision: 1.2
      simulation_time_step: 0.1
      min_points: 6
      visualize: False
      enabled: True
    observation_sources: ["scan"]
    scan:
      type: "scan"
      topic: "/scan_lidar"  # 跟 amcl/costmap 一致, 避开 ESP32 空 /scan
      min_height: 0.05
      max_height: 0.30
      enabled: True
